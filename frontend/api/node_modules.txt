To install the required node modules for the API functions, run:

npm install formidable@2.1.2 node-fetch@3.3.2 --no-save

The --no-save flag is used because we're installing these modules directly in the api folder rather than using a formal package.json setup.